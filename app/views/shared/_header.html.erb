<nav class="navbar navbar-dark bg-inverse">
	<div class="d-flex justify-content-between align-items-center">
		<div id="branding">
			<h1>
				<%= link_to root_path do %>
					Wescom <span>Photos</span>
				<% end %>
			</h1>
		</div>
		<div id="cart">
			<%= link_to( cart_label_quantity_items(), cart_path(), :class=>"btn btn-success hidden-sm-down") %>
			<%= link_to( cart_label_quantity(), cart_path(), :class=>"btn btn-success hidden-md-up") %>
		</div>
	</div>
</nav>